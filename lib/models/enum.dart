enum MenuMealType {
  LOW,
  BALANCE,
  HIGH,
  VEGETARIAN,
}

enum OrderItemType {
  MENU_MEAL,
  CUSTOM_MEAL,
  WEEK_MEAL,
}

enum IngredientType {
  PROTEIN,
  CARBS,
  SIDE,
  SAUCE,
}

enum OrderStatus {
  PENDING,
  CONFIRMED,
  PREPARING,
  SHIPPING,
  DELIVERED,
  CANCELLED,
}