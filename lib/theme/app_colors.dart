import 'package:flutter/material.dart';

// Theme constants for Green Kitchen app
class AppColors {
  // Primary colors from GreenKitchenWeb
  static const Color primary = Color(0xFF4C082A); // Dark red
  static const Color primaryHover = Color(0xFF6D0B3A); // Lighter red for hover
  static const Color secondary = Color(0xFF1CC29F); // Green
  static const Color background = Color(0xFFFAF5E8); // Light cream
  static const Color backgroundAlt = Color(0xFFF5EDD8); // Slightly darker cream

  // Text colors
  static const Color textPrimary = Color(0xFF4C082A);
  static const Color textSecondary = Color(0xFF5F6C7B);

  // Input field colors
  static const Color inputFill = Color(0xFFFFFFFF);
  static const Color inputBorder = Color(0xFFE0E0E0);

  // Additional colors for consistency
  static const Color accent = Color(0xFFFF6B35); // Orange for buttons
  static const Color shadow = Color(0x1A000000); // Subtle shadow
}